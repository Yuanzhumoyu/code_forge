refactor(forge-dsl): P2 GlobalAddr 指令特判模型化——global_reloc 声明驱动

- Instruction 模型新增 `global_reloc` 字段（abs8 | pcrel_hi | pcrel_lo），
  validate.rs 校验取值
- gen_encoder：删除按指令名特判（MOVABS_GLOBAL/AUIPC_GLOBAL/ADDI_GLOBAL），
  改为按 `global_reloc` 字段生成 reloc arm（abs8 → 尾部 8 字节 ABS8；
  pcrel_* → 定宽起始 Relative(4,0)，patcher 按 opcode 分写 hi20/lo12）
- TOML 声明：x86 MOVABS_GLOBAL + riscv AUIPC_GLOBAL/ADDI_GLOBAL
- 生成器不再知道具体 ISA 指令名（新增 ISA 的 GlobalAddr 只需 TOML 声明）

等价验证：golden 全套无 FAILED（MOVABS/AUIPC/ADDI 编码不变）、
riscv 矩阵 126 passed（GlobalAddr 用例真执行）、forge-dsl 46、clippy 0。
