refactor(forge-dsl): P2b 缺省值集中文档化——x86 缺省语义清单

integration.rs 文件头新增"x86 缺省语义"文档块：列出 [abi].move_inst /
ret_mov_inst / call_inst 缺省（MOV_RM8_R64 / CALL_RIP_REL）、浮点移动
MOVSD/MOVSS、尾声跳转 JMP_REL32、条件码/前缀扫描缺省集，并说明
riscv/demo 如何在 TOML 显式覆盖。行为不变，仅组织性。

P3（field_ctor_expr 合并 / token 表派生）评估后放弃：field_ctor_expr 与
field_ctor_expr_view 语义不同（主视图 vs 宽度视图），合并破坏行为；
token 分类跨生成代码/编译期结构，派生收益边际——不为重构而重构。
