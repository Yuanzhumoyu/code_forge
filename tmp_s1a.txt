refactor(forge-dsl): Step 1a——effect 语义标签统一（删 MOV 前缀启发式）

- machine.rs：is_move 改为 effect 含 "Move" 派生（1 def + 1 use），
  **删除 MOV_/MOVR 指令名前缀启发式**（语义与指令名解耦，LLVM TableGen
  flags 同思路）；EffectKind 接线 "Trap"、"Move"→Pure
- model.rs：删除 `move_inst: Option<bool>` 字段（effect Move 取代，破坏性）
- TOML：x86 12 条纯 reg→reg copy（MOV_R_RM/MOV64_RR/MOVSD_XMM_FREG 等）
  加 effect=["Move"]；riscv MOV_RM8_R64(mv) 加 Move、EBREAK 加 Trap
- 行为修正：内存形式 MOV（MOV_R_MEM 等）不再被 is_move 误判（原前缀
  启发式 def/use=1/1 会把 reg 基址 use 误当 copy 源）
- 测试：move_inst_flag_parses → effect_move_label_parses

等价验证：forge-dsl 49、forge-codegen golden 全套无 FAILED、
forge-tests 34（riscv 矩阵 126 passed）、clippy 0。
