[toolchain]
# P0-22 注：钉版 nightly 建议（nightly-2026-08-07）因本机 rustup 无法安装
# （Windows Defender 受控文件夹拦截 .rustup 写入）暂回退浮动；Cargo.lock
# 已提交（可复现依赖）。CI 环境应钉版 + 定期 bump（rustc_compat.rs 兜底）。
channel = "nightly"
