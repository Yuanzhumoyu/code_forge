[package]
name = "forge-opt"
version = "0.1.0"
edition = "2024"
description = "Forge Optimizer: extensible IR optimization framework with 15+ passes"

[dependencies]
forge-ir = { path = "../../foundation/forge-ir" }
smallvec = "1"
dashu = "0.6.0"
log = "0.4"
rayon = { version = "1", optional = true }

[features]
parallel = ["dep:rayon"]
