# forge-rustc WORKAROUNDS（机读清单）

> 本文件是 rustc 内部 API / MSVC 工具链 / forge-codegen 行为的**绕法登记表**。
> 每个条目：`[WA-NN]` 编号 + 位置 + 问题 + 绕法 + 根治方向（是否需主库修复）。
> 代码注释引用编号（如 `[WA-01]`）便于 grep 定位；新 workaround 必须在此登记。

## 工具链/链接器层

| 编号 | 位置 | 问题 | 绕法 | 根治方向 |
| --- | --- | --- | --- | --- |
| WA-01 | backend.rs `add_data_with_relocs` / func_ref.rs `vtables` | MSVC 链接器不应用 `.rodata` 段的 ADDR64 重定位（实测链接后全 0） | vtable 指针表放 `.data` 段（`add_data_with_relocs`） | 需主库：COFF writer 对 `.rodata` 重定位的兼容或文档化 |
| WA-02 | backend.rs `keep_item`（build_plans 过滤） | alloc crate 的 `__rust_alloc` 等 shim 实例若编译，与手写 shim（alloc_runtime.rs）重复定义 → LNK2005 | `collect_instances` 过滤 alloc_shims 名单 | 无（设计取舍：手写 shim 绕过 Layout/Alignment lowering） |
| WA-03 | backend.rs `MonoItem::GlobalAsm` | global_asm 无实现 | 直接 `dcx().err`（编译失败而非静默） | 主库：GlobalAsm → 对象文件段支持 |
| WA-04 | README（用法） | `-Zshare-generics=yes` 下 core/alloc 泛型实例的辅助函数（`is_null`/`precondition_check`）LLVM 侧未生成 → LNK2019 | 依赖 LLVM 侧生成；文档列为已知残余 | 主库：补生成"被引用但上游未生成的实例" |
| WA-05 | README（用法） | 常规 `+`/`-`/`*` 的溢出 Assert 分支未覆盖（checked 元组结果本身已实现：`sadd_overflow` 等 API + tuple 拆写完整路径） | 测试统一 `-C overflow-checks=off` | 主库：常规算术溢出 Assert 语义（checked 路径已通） |

## 布局/降级层

| 编号 | 位置 | 问题 | 绕法 | 根治方向 |
| --- | --- | --- | --- | --- |
| WA-06 | layout.rs `scalar_pair_offsets` | niche ScalarPair（`Option<i32>` 等）的 `fields()`/`variants` 缺字段（rustc_abi 越界 panic） | 优先用 `BackendRepr::ScalarPair` 标量宽度（第 1 标量@0、第 2 标量紧随） | 无（rustc 内部布局 API 行为，跟随 nightly 适配） |
| WA-07 | backend.rs（链接） | MSVC 链接器需要 `__CxxFrameHandler3` | `-C link-arg=/DEFAULTLIB:vcruntime.lib` | 无（工具链要求） |
| WA-08 | backend.rs `join_codegen` | rustc 增量编译需要 WorkProduct 元数据 | 实现 `join_codegen` 输出 `WorkProductMap` | 已实现（非 workaround，随 rustc trait 演进） |
| WA-09 | lower/mod.rs `build_signature` vs `lower_body` | sret 判定在签名构造与入口收参两处独立实现，不一致会导致收参错位 | 两处共用 `is_agg_mem` 判定（注释互相引用） | 已消除（单一判定函数） |
| WA-10 | lower/mod.rs `mono_symbol` / `new` | 闭包/内部 shim 的 DefId 无 `item_name`（直接调用会 ICE） | 用 `tcx.def_path_str` 安全取名字符串 | 无（rustc API 行为） |
| WA-11 | forge-codegen 主库分支/选择降级 + forge-rustc `lower/` | Vec grow 链运行期值错（e2e `vec_push`/`vec_string` known_failure） | **2026-08 十九轮定性**：`Vec::new` 构造全对（RawVec 新嵌套布局 cap@0/ptr@8/len@16）；`with_capacity+push` 全对；**new+push（触发 grow）错**：cap=0（应 1）、len=5（应 1）；**maxmin 实验证明 `Ord::max/min` 本身降级正确**（0.max(1)=1）——cap=0 根因收敛为 grow_amortized 的 new_cap 计算/实参错；vecwc2（with_capacity(2)+2 push）挂起 124 | **已关闭（2026-09）**：grow 链（Result/ControlFlow/TryReserveError 嵌套 niche 错误传播）累积修复收官——field_offset Primitive 防护 + 窄类型宽度（write_bytes_loop）+ sret 计数/Ignore 参数 + **WA-29**（Niche CONSTRUCT 写入宽度按 tag 标量，8 字节指针 tag 恒 movl 残留高 4 字节 → 判别误判）。`vec_push`/`vec_string`/`vec_from_slice`/`string_concat_len` 全部转正（e2e 85/85） |
| WA-17 | 主库 `[lower.Select]`（isa TOML）+ regalloc | Select 降级无条件 true 臂（cf5 嵌套 niche 判别曾全错） | **2026-08 已修规则层**：x86 `[lower.Select]` 从 `mov rd, rs2` 改为 `mov rd, rs3; test rs1, rs1; cmovne rd, rs2`（条件选择）；aarch64 同步（SD_CMP+SD_CSEL NE）；riscv64 标记 TODO（无 csel，未验证目标）。**2026-09 恢复原生 Select（builder.select）验证**：新增 `test_jit_select_strict_matrix`（5 组探针：变量臂/任意 cond/I32/链式/多 XReg 压力全过）证明主库 [lower.Select] 无 bug；rvalue.rs niche 判别从算术公式恢复原生 `builder.select`，e2e 85/85 全绿（含 nested_enum_break 等 niche 用例） | **已关闭**：主库 [lower.Select] 正确（严格矩阵验证），原生 Select 已恢复 |
| WA-18 | 主库 AtomicRmw（isa TOML `xadd {g1}, [{0}]`）+ forge-rustc static 落段 | 原子 RMW（fetch_add 等）运行期 SEGV——**2026-08 定性为两因叠加**：①内部可变 static（`static A: AtomicI32`，UnsafeCell）被 `is_mutable_static`（仅语法 mut）判为只读 → 落 `.rodata` → `xaddq` 写只读页 SEGV（di_atom gdb 实证 rip=0x…14e7、r11=&A 在 RVA 0x3000）；②JIT 高压测试（10 存活值 + atomic_rmw）证明主库 regalloc 无 spill 缺失（原假设推翻） | **已关闭**：forge-rustc 侧 `global_data.rs` 落段判定改为 `is_mutable_static \|\| !Ty::is_freeze`（内部可变 → `.data`）；intrinsics.rs 解除 RMW 门控——Xchg/Add/Sub 转正（`atomic_rmw_gated` 负用例删除，新增 `atomic_fetch_add`/`atomic_xchg`/`atomic_fetch_sub` 正用例）；And/Or/Xor/Nand/Max/Min/Umax/Umin/Cmpxchg 仍编译期拒绝（需 CMPXCHG 循环/cmpxchg 指令扩展） | 已关闭 |
| WA-19 | backend.rs `build_plans`（build-std 全量场景） | `-Z build-std=core,alloc` 把 forge 当全量后端编译用户依赖（smallvec/serde/keccak 等），intrinsic 长尾（已补 ~20 个：bswap/cttz_nonzero/caller_location/compare_bytes/typed_swap_nonoverlapping/copy/fabs 等）+ AtomicRmw 门控阻塞 | 依赖的 intrinsic 已逐步补齐（**2026-09 补 `integer_min/max`**——rustc 1.100 的 GlobalAlloc::realloc 引入，按 T 符号性 + 原生 Select 实现）；atomic_compare_exchange 被 WA-18 门控拒绝；闭包 item_name ICE 已修（opt_item_name 兜底） | 主库：WA-18 根治后 atomic 解锁；build-std 全量编译依赖作为远期（非本后端目标场景，e2e 子集为主） |
| WA-20 | forge-rustc 生成层（Range 构造+deref+写回+ScalarPair 组合） | `nested_loop_break_outer` 挂起 + 多次 next 值错（2026-08 定性进展）：哨兵实证 **0..1 的第二次 next 返回 Some(0)（应 None）**——start 不递增；spec_next/next 链反汇编**完全正确**；**主库 JIT 三层探针全部通过**（test_jit_cross_call_writeback / two_level / writeback_with_inner_call：跨调用写回 + 中间调用 clobber 均正确）——**主库排除**，问题收敛到 **forge-rustc 生成的特定 IR 组合**（Range Aggregate 构造 → &mut 中转 → deref 读 → 写回 → ScalarPair 返回链） | **已关闭**：根因即 WA-23——next 返回 `Option<i32>` 是 ScalarPair，main 收返回时 `pack_sp` 对 hi 字段（value@4）8 字节写越界覆盖相邻 `_4` 槽（Range.start）→ start 被垃圾覆盖 → next 永返 Some(0) → 循环不终止/值错。按标量宽度读写后 range_next_twice/nested_loop_break_outer 转正（LLVM 实证后者真实语义 15，原 expected=12 注释手算错误） | 已关闭 |
| WA-21 | 主库/forge-rustc static mut 访问（帧/block 布局） | `static mut X` 写崩（2026-08 定性进展）：`_1 = const {&X}` 的 **store 指令存在**（反汇编 140001320 movabs 0x140004000 → store -0x50；VCode ProgPoint 216-220 确认）——但**运行期 _1 槽读垃圾（0x7ff6…）**→ 写 [垃圾] SEGV；固定基址（/DYNAMICBASE:NO）仍崩（非 ASLR）；崩溃路径（0x1400024xx，`(*_1) = move (_2.0)`）load _1 槽得垃圾——**疑似不同 block 的帧/槽布局不一致或 -0x50 被覆盖**（机制待查）；简单形态（atomic fetch_add 的 &A）正常；**vec_push（static mut HEAP allocator）偶发 SEGV 同因**（5 次单跑全过、全量偶发 FAIL） | **已关闭**：根因即 WA-22——`X += 7` 是 AddWithOverflow，其 flag 槽（(i32,bool) 聚合 @+4）8 字节 store（movq）覆盖相邻 `_1`（&X）参数/局部槽 → p 被写 0 → 写回 `(*_1)=move(_2.0)` store [0] SEGV。flag 读写对称窄化（ireduce I32 + 32 位读）后 `static_mut_write` 转正（exit=12）。vec_push 偶发同源（HEAP 写回前 flag 覆盖） | 已关闭 |
| WA-22 | forge-rustc `lower/statement.rs` checked 算术 flag 写 + `lower/rvalue.rs` load_ty | checked 算术（AddWithOverflow 等）结果 `(value, bool)` 的 flag 槽：**主库对 BOOL 的 store/load 均按 64 位（TOML Load/Store 规则仅 32/64 两档，BOOL bits=1 落默认 mov_mem/mov_sto）**——flag@聚合+4 的 movq 8 字节写覆盖相邻槽（bump1 实证：覆盖 _1 参数槽的 p → 写回 store [0] SEGV；读侧 movq 读 -0x54..-0x4d 混入相邻槽 → testq 非零误判溢出 → panic 路径死循环） | **已修**：写侧（statement.rs）`ireduce(f, I32)` 再 store（4 字节 mov_sto32）；读侧（rvalue.rs load_ty）BOOL 显式 32 位读 + ireduce 回 BOOL（4 字节 mov_mem32，槽内恒 0/1 不越界）。bump1/2/3 哨兵全过（exit 42），`static_mut_write` 转正（WA-21 关闭） | 已关闭（读侧语义与窄读等价；若主库未来补 BOOL 窄规则可移除特判） |
| WA-23 | forge-rustc `lower/place.rs` pack_sp/unpack_sp + `layout.rs` | **ScalarPair 打包/解包恒用 I64（8 字节）读写字段**：`Option<i32>`（tag@0、value@4 各 4 字节）的 hi 字段 8 字节写越界覆盖相邻槽（range1 for 循环挂起实证：main 收 `next()` 返回 `pack_sp` 的 hi 写 -0x6c..-0x65 覆盖 `_4` 槽 -0x68 的 Range.start → start 变垃圾 → next 永返 Some(0) → 循环不终止；unpack 8 字节读同样混入相邻槽垃圾高位）——WA-20 真正根因，与 WA-22（BOOL 64 位越界）同族 | **已修**：`scalar_pair_widths` 从 `BackendRepr::ScalarPair{a,b}` 取标量真实字节宽度，`pack_sp` 按宽度 ireduce + store（窄字段 4 字节写）、`unpack_sp` 按宽度 load + uextend（不混入相邻槽）。range1 exit=6（0+1+2+3）、nex1=256/nex2=257/nex3=119（0..1 第二次 next 正确 panic）、range_next_twice/nested_loop_break_outer 转正（e2e 85→83 known） | 已关闭 |
| WA-24 | forge-rustc `lower/mod.rs` SwitchInt const 判别折叠 + reachable_blocks | **switchInt 判别为编译期常量（Operand::Constant 可求值 / RuntimeChecks）时不折叠**——两分支都生成。`slice::iter::Iter::next` 的 `switchInt(UbChecks) -> [0: bb11, otherwise: bb10]`（UbChecks = `core::intrinsics::ub_checks` const 求值，runtime 恒 false）若 bb10 也生成，其 `unchecked_sub::precondition_check` 调用引用 core rlib（LLVM 预编译）缺失符号 → LNK2019（slice_iter_sum/vec_iter_enumerate 编译失败） | **已修**：SwitchInt 折叠 const 判别只生成目标分支 jump；lower_body 改可达性 BFS 驱动（预创建/遍历都跳过不可达块——否则 finish 断言"块无终结符"）。对齐 rustc 官方 PR #122282（非标准后端消除 UbCheck 分支）。slice_iter_sum 转正（exit 10）；`-Zub-checks=no`/`-C debug-assertions=off` 无效（UbChecks 常量 MIR 已固化，不折叠则仍生成） | 已关闭 |
| WA-25 | forge-rustc `lower/statement.rs` Rvalue::Ref 对 fat pointer | **`Rvalue::Ref`（`&*fat_ptr`）恒返回 place 槽地址（8 字节 ptr）**——目标类型是 `&[T]`/`&str`/`&dyn`（16 字节 ScalarPair：ptr@0 + len/vtable@8）时只写 ptr 半区，len 半区不写 → `PtrMetadata` 读 len=0（Vec deref 后 `sl.len()`=0 实证；`Iter::new` 的 fat ptr 构造依赖此） | **已修**：statement.rs `_ =>` 分支对 `Rvalue::Ref` + size>8 按 size 整值复制（copy_agg dst←src 16 字节），与 Use/Cast 的聚合复制一致。deref1（Vec→&[i32]→len）从 len=0 变正确（3 push 后 len 路径通）；vec_iter_enumerate 从 LNK2019+SEGV 变运行期值错（want 80） | 已关闭（fat ptr Ref 语义） |
| WA-29 | forge-rustc `lower/statement.rs` Niche CONSTRUCT 写入宽度 | **niche 枚举构造写 niche_value 恒用 I32（movl 32 位）**——8 字节指针 tag（Option<(usize,&i32)> 的 &i32、Option<&T>）只清零低 4 字节，高 4 字节残留栈上旧值；判别读（rvalue.rs WA-26）按 I64（8 字节）load + uextend → 读出的"null" ≠ 0 → None 误判 Some（enumerate None 路径 SEGV 根因：Enumerate::next 的 None 构造 movl 写 _0+8，main 判别读 8 字节含残留高位 → 误判 Some → `**x` 解引用 null；IR 层/最小探针（en21 mk_none）全对、仅复杂场景错位——栈高 4 字节运气归零时掩盖） | **已修**：写入宽度按 tag 标量宽度（BackendRepr::Scalar/Pair 的 primitive size）——8 字节 tag 写 I64（movq 完整清零），窄 tag（u8/u16）保持 I32 不越界。en17 手动多 next 从 SEGV 转正（exit 0x56）；**vec_iter_enumerate 转正（exit 80，e2e 85/85 全绿）** | 已关闭 |
| WA-31 | forge-rustc `src/dwarf.rs`（C1/C2 DWARF 发射，2026-09 排查） | **DWARF 发射一系列结构错误**（objdump/gdb 拒解析或错位，逐一定位）：①version 是 u16 只写 1 字节——`.debug_info` 的 abbrev_offset 错位（addr_size 08 混入 → "bad offset 0x8000000"）、`.debug_line` version 高位吃到 header_length 首字节 → 0x0f02=3842；②DIE 值流误写属性名——frame_base 前多 `0x40`、location 前多 `0x02`（读者把 0x40 当 exprloc 长度 64 吞掉全部 DIE → "Abbrev 83 不存在"）；③行程序头不合法——opcode_base=1 使标准操作码（copy/advance_line/set_file）被当 special opcode、v4 头缺 max_ops_per_inst、`advance_line` 操作码字节漏发（只发 operand）、`set_file 0`（文件号应从 1 起）、文件条目缺 dir/mtime/length + 表终止符；④CU 缺 low_pc/high_pc/stmt_list + 无 .debug_aranges——gdb 16 的 pc→CU 映射空 → 帧落回 COFF 符号（`__end__`）；⑤文件表/CU 名 = crate 名（匹配不到磁盘 .rs——`list` 失败） | **已修（2026-09）**：①version `push(4);push(0)` 双处；②删除 DIE 值流里的属性名字节（exprloc = len+内容）；③行程序头对齐 clang `--dwarf=rawline` 参考（v4：max_ops/line_base=-5(0xfb)/line_range=14/opcode_base=13 + 12 长度表、文件条目补全、set_file 1、补 `DW_LNS_ADVANCE_LINE` 0x03）；④CU 加 low_pc（reloc 首函数）+ high_pc（data8 总长+0x200 边距）+ stmt_list=0，新增 `.debug_aranges`（单 CU 范围）；⑤CU 名/文件条目用真实源文件路径（source_map 根文件）。验证：objdump `--dwarf=info/decodedline/rawline` 全净（lld-link 产物）；dwarf 结构单测 7 个（逐字节解析 info/line/aranges）；gdb 16.2 能读符号/设断点命中/`list` 出源码。**残余（未关闭）**：gdb 交互 `info args/locals`/单步在 PE 未通（需 .debug_frame CFI；lld-link 不产 COFF 符号表 → gdb 无 minsym、入口地址与 `__end__` 冲突——验证用 GNU ld 链接）；MSVC link.exe 截断段名 `.debug_l*` → 验证用 lld-link/GNU ld | **已关闭**（v4/v5 结构全对：objdump info/decodedline/rawline 净；gdb 源码断点/单步/变量槽位实证） |
| WA-32 | forge-rustc `src/dwarf.rs`（v5 升级 + gdb 集成，2026-09） | **w64devkit gdb 16.2 对 PE 只支持 DWARF5**：gcc 同源代码 -gdwarf-4 编译后 gdb 同样全失效（断点不解析/无行号——对照实证），默认 v5 正常。v4 下此前全部 gdb 问题皆为版本所致 | **已修（2026-09）**：升 DWARF v5——`.debug_info` 头 unit_type(1)+address_size(1)+abbrev_offset(4)；`.debug_line` v5 头（address_size/segment_selector）+ LNCT 文件表（format_count/count 前缀——dir 空表须 format_count=0+count=0 两字节；**line_strp 偏移 4 字节**非 uleb；**文件表两条同串条目**（file 号 1 在 binutils 0 基 / 寄存器初值 1 基下都命中，单条目会被判越界）；新增 `.debug_line_str` | 关闭 |
| WA-33 | forge-rustc `src/dwarf.rs`（subprogram 范围/行序列，2026-09） | **①subprogram 缺 DW_AT_high_pc**：gdb 需函数结束地址建 function block——缺则变量/参数 DIE 全部被丢（对照 gcc：block 带范围含符号）；②**行程序每行一个 sequence**：每行零跨度 → gdb 丢弃零长度行 → 行号全空（对照 gcc/clang：每函数一个 sequence，行间 advance） | **已修（2026-09）**：①subprogram 加 high_pc（data8 = 函数代码字节——backend.rs 逐实例收集 fn_sizes）；②行程序每函数一个 sequence（decl 行 + per-statement 行 + 一次 end_sequence；行寄存器 sequence 起点 1、行内 advance_line 累加 delta）——objdump decodedline 净；gdb：function block 带范围、变量符号入块（`info scope` 显 y@rbp-80，`x/wd $rbp-80`=42 端到端正确）、源码断点/`list`/`next` 全通。**残余**：ref4 类型跟随（print 显示 unknown type，ptype i32 正常）+ 序列终端行 line 0——gdb 16.2 PE DWARF5 边界问题，路线图待续（可能需 .debug_frame 对齐 gcc 形态） | 已关闭（M10 增补见下：类型打印根因 = abbrev DW_AT_type 表单误用 data4） |
| WA-33 增补（M2） | forge-rustc `.debug_frame` CFI + 行表终端行（2026-09） | **①无任何 CFI**：gdb 16.2 amd64-windows 先 SEH(.pdata)、无条目落 dwarf2-frame(.debug_frame)——forge 两者皆无 → **bt / info args / info locals 全空的根因**（对照：gcc PE 产 .debug_frame 且 bt 正常）；②**序列终端行 line 0**：gen_debug_line 末行 copy 后直接 end_sequence → 末行区间 [X, X) 零宽被 gdb 丢（对照 gcc decodedline 末行 end = fn 末地址） | **已落地（M2，2026-09）**：①**CFI 子系统**——forge-codegen 新 `machine/cfi.rs`（架构中立能力层：`CfiOp`/`FunctionCfi` 数据模型 + 按 ISA 名注册/懒派发的 prologue 扫描器表，`scan_x86_prologue`：对发射机器码校验 15 字节 x86 prologue 前缀 `55 48 89 e5` + 7 次 callee-saved push，行键 = 指令结束偏移，CFA=rbp+16 定帧 + 保存槽 rbp@-16/RBX@-24/RDI@-32/RSI@-40/R12@-48/R13@-56/R14@-64/R15@-72；前缀不符 → None 安全退化）+ `CompiledFunction.cfi`（emission 完成后填充，riscv64/demo None 零回归）+ dwarf.rs `gen_debug_frame`（CIE version1/RA 列 16（gcc 用 32 是 PE 列，gdb 不依赖）/def_cfa rsp 8 + offset RA@CFA-8；FDE = length 回填 + CIE_pointer 0 + initial_location 占位 + ADDR64 reloc→函数符号 + address_range=fn_sizes + DW_CFA 行流（advance_loc/def_cfa_offset/def_cfa_register/offset=槽字节/8）；NOP 补条目到 8 对齐——gcc 字节形态实证对照）+ backend.rs 按 fn_sizes 同键控收集 → build_dwarf_sections 新增段；无 CFI 的 fn 不产 FDE；debuginfo 关闭整段不产；②**终端行修复**——gen_debug_line 加 fn_sizes 输入，末行 copy 后、end_sequence 前发 `DW_LNS_advance_pc`（uleb = fn_end - 末行 addr，delta=0/大小未知省略）→ 末行延伸到函数真实末地址。验证：dwarf 结构单测 +2（frames_structurally_decodes/line_end_advance_to_fn_end，共 12 全绿）；e2e `debuginfo_full`/`debuginfo_line_tables` 编译运行 exit-code 不回归；lld-link 产物 objdump `--dwarf=frames` 净（FDE 行流 = CFA=rbp+16 + 全保存槽，地址已解析绝对 VA）+ `--dwarf=decodedline` 末行 end = fn 末；**gdb 16.2 `bt 3` = helper ← mainCRTStartup 双帧**（返回地址经 CFA-8 读）+ `info args`/`info locals` 列出变量。**残余（未关闭）**：ref4 类型跟随（`p y` 显示 < unknown type >；`ptype i32`/转型路径 `p (int)y` 正常——位置语义全对，与 CFI 无关）——类型打印 M 对照矩阵待做（M10 gdb 自诊 + M1 字节 diff；全阴性则上报 gdb） | 已关闭（M10 增补见下：CFI 后类型打印 = abbrev 表单 bug，已根治） |
| WA-33 增补（M10） | forge-rustc `src/dwarf.rs` `gen_debug_abbrev`（DW_AT_type 表单字节，2026-09 M10 归因） | **`p y` 显示 "< unknown type >"（`ptype i32`/`info types` 正常、位置语义全对、转型 `p (int)y`=42）的真正根因**：DW_AT_type(0x49) 的属性 form 字节误写 **0x06 = DW_FORM_data4**（常量类）而非 **0x13 = DW_FORM_ref4**（引用类）——abbrev code 4/5/7/9 四处。DWARF 规范：DW_AT_type 必须是 reference 类；data4 只是无符号常量。objdump 宽容（按 4 字节读出值 0x1bf 照常显示，自洽假象）；gdb 严格：`die_type` 读到非引用 form → "During symbol reading: DWARF Error: Bad type attribute DW_AT_type in DIE at 0x…"（M10 自诊 `set complaints 10` 首屏即现）→ 该变量/参数类型落 error type → `p y` 拒（"y has unknown type; cast it to its declared type"）、`info scope` length=0、`maint print symbols` 显示 `<unknown type …DIE 0x1ae>`。`typedef i32` 仍注册成功（base_type DIE 被线性遍历读到）故 `ptype i32`/`info types` 正常——症状与"跨 DIE 引用失败"表象一致但机制是本字节错误（历次 WA-33 猜因"gdb 读 PE DWARF5 的 ref4 跨 DIE 引用失败"不成立） | **已修（M10，2026-09）**：①code 4/5/7/9 的 `DW_AT_type` form 0x06 → **0x13（DW_FORM_ref4）**（值流字节不变——ref4/data4 同为 4 字节，仅 abbrev 表单语义不同）；②**配套修复**：所有类型引用（变量/参数、pointer pointee、结构成员）**绝不写 ref 0**（0 = CU 头偏移非 DIE——gdb 报 "Cannot find DIE at 0x0" 并拒整个 CU；被 data4 形态掩盖的 &PanicInfo 等未建模 pointee 在表单修复后即刻暴露）——未解析引用终局回填到新增 `DW_TAG_unspecified_type`（code 12，tag 0x3b）；新增回归单测 2 个（`abbrev_type_attr_uses_ref4_form`：断言所有 0x49 属性 form==0x13；`unresolved_type_refs_fall_back_to_unspecified`：未建模引用 → unspecified 偏移且恒非 0）。对照实证：gcc -gdwarf-5 同形 C 探针 abbrev 字节 `49 13`；修复后 objdump `--dwarf=info/abbrev` 显示 `DW_FORM_ref4` + `DW_TAG_unspecified_type`、**gdb 16.2 `p y` = 42（c/rust 语言均）、`ptype y` = i32、`info scope` length=4、complaints 清零**（见 README 验证路径） | **已关闭**（WA-33 全条目关闭：gdb-PE 类型打印恢复正常——路线图最后开放项命中并根治） |
| WA-34 | forge-rustc `layout.rs` struct_members + dwarf.rs | **rustc 2026 field.ty 的 Debug 形态带未归一化包装**：`format!("{:?}", field.ty(tcx, substs))` 输出 "Unnormalized { value: i32, .. }"（归一化重构后字段类型处于 unnormalized 状态）——与变量侧 ty_desc（"i32"）不匹配 → structure_type 成员的 DW_AT_type 回填失配（=0 占位） | **已修（2026-09）**：`layout.rs normalize_ty_debug` 剥壳（strip "Unnormalized { value: " + 取到 ", " 前）与变量侧对齐——member type ref4 正确指向 base/pointer DIE（objdump 实证 x@0/y@4 → 0x17c i32） | 半关闭（剥壳只覆盖单层标量形态；泛型/带逗号内层形态随 rustc 演进补） |
| WA-35 | 主库 x86 `Cmpxchg` 窄域 + forge-rustc 原子 fetch 族 CMPXCHG 循环 | **IR 层 cmpxchg 重试循环（fetch_and/or/xor/nand/max/min/umax/umin）与主库窄域 Cmpxchg**：JIT 隔离实证——全 I64 域循环本就通过；I32 域必失败（rm_mem 形式宽度编码恒取寄存器侧 64——4 字节原子 8 字节访问越界 + 比较错 → 写丢失/负值符号错乱）。初代修法（v13）：固定 `_32` 变体惯例 | **已关闭（v14，DSL 根因修复）**：断点定位 = regalloc 回填 `set_reg_field` 用**槽静态 class** 重建物理 Reg（多类槽 gprx `class=None` → 兜底 `GPR(8)` 恒 64 位视图）+ 结果 XReg 预分配硬编码 GPR64。修复：①`MachineInst::set_reg_field` 加 class 参数，单类槽仍按槽 class（指令声明宽度）、**多类槽用 XReg.class**（IR 值宽度）重建；②`pipeline/lowering.rs` 结果 XReg 按 `reg_class_for` 分派（I32→GPR(4)）。`CMPXCHG_MEM_R` 数据槽改 `gprx`、删 `CMPXCHG_MEM_R_32` 与 rs2_width==32 分派——5 cmpxchg JIT 守卫（i32 单发/循环/参数域 + i64 + 邻字节哨兵）全绿（auto 32 位无越界）。**v14 后续关闭**：①字节宽度（e8e202d）——8 位指令（8A/88/0F B0）+ 窄 load movzx + fetch_add/sub/swap 窄域循环化，u8/bool/16 位原子 e2e 全绿；②cxchg/cxchgweak 双结果（f9d696f）——atomic_cxchg 返回 (old, icmp(eq,old,expected)) 两值走 pack_sp ScalarPair；③WA-36 窄 signed 值符号扩展链路（2039c4c）；④AtomicRmw {g1}→{out}（XADD/XCHG dest 用结果 XReg——i32 快路径 32 位无越界，jvm 哨兵用例验证）。**无残余**（窄域 fetch 循环、≥4 字节快路径全正确） | |
| WA-36 | forge-rustc 窄 signed 值符号扩展链路（v14 字节宽度解锁后暴露） | **①窄 signed 值 32 位域符号扩展缺口**：i8 load（movzx 零扩展）把 -50(0xCE) 表示成 +206，signed 比较/cast 错；②u8/i8 **高字节常量直接比较**错误（常量恒 signed sext：0xFC→-4 与值 252 不等） | **已关闭（v14）**：①isa Sextend 8/16 位源按 movsx（0F BE/BF）分派（原恒 movsxd 32 位源）；②rvalue.rs 常量求值按 rustc 类型符号性扩展（u8 0xFC→252 零扩展、i8 sext）；③icmp 窄操作数按 signed sext/zext 到 64 统一比较（x86 cmp 64 位域）；④IntToInt 窄 signed→宽 sext（原 ireduce 截断给 +206）；⑤intrinsics atomic_rmw_loop SMax/SMin 窄域 sext。i8 fetch_max/min + u8 高字节常量直接比较 e2e 全转正（96 用例零 known_failure） | |
| WA-37 | forge-rustc SIMD/宽向量 ABI（2026-09 调研登记） | **YMM ABI 主库侧已实现**（ed43103/e9e869a/33df6fa/4959474）：>128 位向量 by-ref 传参 + sret 返回全链路 + 7 个 JIT 测试全绿（v256_byref_param/wide_vector_call_byref/v256_byref_return/wide_vector_call_sret/mixed_scalar_and_byref_args/sret_with_byref_arg/v512_byref_param）。**残余缺口**：②**≤16B 向量按值 XMM 全宽移动缺失**：V128 参数走 MOVSD/MOVSS（8/4B）静默截断——**B3 门控分级后 V64/V128 仍编译期拒绝**（simd_abi_gated 负向守护）；③V512 测试仅断言 lane0（盲区）；④宽向量第 5+ GPR 槽显式 Unsupported（两侧对称，建议不做）；⑤CallIndirect 宽参/返回缺测试 | **D2 已关闭（2026-09 基建，探针转正）**：**V256（Simd<f32,8> 32B）按内存间接 ABI 放行**——①`abi.rs`：rustc `BackendRepr::SimdVector` >16B 归类 `Indirect`（Windows x64 无 YMM 参数寄存器；rustc FnAbi 实证 `Simd::load` ret.mode=Indirect——forge 与 rustc 判定一致）→ V256 参数/返回走通用聚合槽路径（by-ref arg 指针 + 收参 `copy_agg` / sret ret 缓冲），值全程内存建模、不物化向量寄存器值；②`lower/statement.rs` CopyNonOverlapping **>8B 元素全宽块拷贝**（portable_simd `Simd::load` 的 32B memcpy 旧实现只搬 8B → lane2.. 垃圾；按元素 sz 静态展开 8B 块）；③`lower/mod.rs` B3 门控分级：>16B（V256）放行、≤16B（V64/V128）保持拒。simd_v256_local=108 / simd_v256_call=1001 转正。**D3 已关闭（2026-09，WA-37 收官）**：**≤16B 向量（V64/V128）按值 XMM 全宽放行**——①主库 v12 DSL：ABI 键 `[abi].vec_mov_inst`（缺省 "MOVAPS"）——收参（@move_args，frame.rs）/调用实参/Return/ret_move 对 **VEC(16) 类**（参数类判定或 ≤16B 向量类型判定）改用**全宽 128 位 MOVAPS** 而非 MOVSD/MOVSS（实证：V64 参数 lane1 恒 0——调用方把值塞 GPR 槽、callee 从 XMM0 收参槽位错位；V128 lane3 靠 movsd 合并语义 + 遗留 XMM 高半偶然存活——真缺陷）；②新增 Load/Store 谓词 `rd_vec`/`rs1_vec`（向量类型字节数，Store 无结果故不能用 elem）+ 新指令 MOVUPS_128_R_MEM/MOVUPS_128_MEM_R（0F 10/11 无前缀，register-indirect 16 字节）——V128 值经栈槽全宽往返（V64 8B 走 movsd）；③forge-rustc：lower/mod.rs B3 门控整块删除（≤16B Direct + >16B Indirect 全放行）、place.rs load_place 自检排除 8B 向量（V64 load 曾 assert 误杀）、simd_abi_gated 负向用例删除；④验证：jit byval 5 用例 + V128/V64 槽往返探针全绿；e2e simd_v128_local=104 / simd_v128_call=4004 / simd_v128_return=71 / simd_v64_call=505 / simd_v256_call/local 不回归。**残余（非 D3 范围）**：③V512 测试仅断言 lane0（盲区）；④宽向量第 5+ GPR 槽显式 Unsupported（两侧对称，建议不做）；⑤CallIndirect 宽参/返回缺测试 | **已关闭（V256 内存 ABI + V64/V128 按值 XMM 全宽，向量 ABI 全线）** |
| WA-38 | backend.rs M4 并行 CGU Stage A（2026-09 M4 根治） | **rustc 1.99/1.100（2026-08/09 nightly，统一 WorkerLocal 查询引擎）下 `TyCtxt` 非 Send，tcx 查询只能在 rustc 自身注册的查询线程池线程上执行**——自定义 std::thread 里任何查询（layout_of/instance_mir/arena 分配等触达 ImplicitCtxt TLS 与 WorkerLocal registry）都会 panic（"no ImplicitCtxt stored in tls" / "Unable to verify registry association"），且**无线程注册 API** → M3 的 std::thread 自建池 + WorkerPayload（raw 指针 unsafe Send）+ 能力探针方案为死路 | **M3 代码结构保留 + M4 并行原语根治**：①任务化结构（不变）——A1 排序实例**每个函数一个任务**（单函数粒度，任务私有 FuncRefTable：@N/G{N} 就地 resolve 不跨函数逃逸零加锁）+ 任务登记条目 `merge_task_table` 归并（vtable/promoted 按 alloc_id 去重、line/var/enum 按全局函数序）+ 主线程按原实例序串行 emission（单对象单模块，产物与 -C codegen-units 无关）；②**M4 并行提交**：任务以 `rustc_data_structures::sync::par_map` 提交 **rustc 查询池**——codegen_crate 本身跑在 rustc 池作业线程上，嵌套池作业继承全套线程上下文（WorkerLocal registry/ImplicitCtxt/SessionGlobals），与 rustc_codegen_ssa 自家 `-Z threads` per-CGU 并行同一原语（与 LLVM 无关）；③**门控** = `FORGE_CODEGEN_THREADS>1 && jobs.frontend.is_some()`（-Z threads>=2 / --jobs-frontend，rustc_interface 据此 set_dyn_thread_safe_mode）&& 函数数>1——无 -Z threads 时走显式串行 map（par_* 非并行模式亦自动串行），**默认 T=1 与旧路径逐字节一致**；④`FORGE_CODEGEN_THREADS>1` 但无 -Z threads 仅 stderr 提示"需 -Z threads>=2（RUSTFLAGS）"，产物不变；⑤每任务 catch_unwind 保留（panic → FnOutcome::Err，主线程统一 dcx 报告）。**残余（M4 文档化）→ M5 已关闭（稳定键命名）**：并行下 rustc 全局 AllocId 计数器（`AllocMap` 内容 dedup 锁 + `next_id: AtomicU64` 首次请求序）随调度序分配 → 内部 slice/vtable 符号名后缀 `__slice_alloc{N}` 数值跨并行运行漂移（实测 96 函数应力源 6/6 运行符号表互异；内容↔符号恒一致、代码/数据段字节与重定位相同、objdump 归一化指令序列与 exit 与 T=1 一致——determinism 矩阵 zt2 变体守护）；串行路径完全字节确定（跨运行 SHA-256 相同） | **M4 已根治**（并行 = 函数任务提交 rustc 自身查询池 par_map，产物行为与 T=1 恒等）；**残余 M5 已关闭（2026-09，WA-38 全条目关闭）**：内部数据符号改**内容稳定键**——`slice_sym`/`vtable_sym` 从 alloc_id 后缀改为记录内容（bytes/align + vtable 追加 trait def_path 语义鉴别键）的 **64 位 FNV-1a 哈希**（`__slice_{:016x}` / `__vtable_{:016x}`，lower/statement.rs + lower/vtable.rs，哈希 helper lower/mod.rs `fnv1a64`）；promoted/slice 因内容命名在 `intern_promoted`/`merge_task_table` 双路**按内容去重**（同内容跨 alloc_id 共享记录，debug_assert 同名同字节/对齐碰撞防护）；符号跨运行/串并行逐字稳定（determinism 新增 `stable_data_symbol_names_across_scheduling`：serial + `-Z threads=4`×3 的对象符号表全等；同套 96 函数应力源实测 6/6 全等）。Stage B → M6 已实施（见 WA-39：每 CGU 独立对象文件 + owner 归并 + 多 WorkProduct） |

## 使用约定

| WA-39 | backend.rs M6 并行 CGU Stage B（每 CGU 独立对象文件，2026-09） | **rustc 非增量默认把过小 CGU 合并（`NON_INCR_MIN_CGU_SIZE=1800`，见 rustc_monomorphize::partitioning）**：单模块小 crate 即使默认 codegen-units=16 也只产出 1 个 CGU → 单对象路径（= Stage A 形态）；多 CGU 需显式 `-C codegen-units` + 多模块/大 crate。rustc 对 -Zcodegen-backend 只调 codegen_crate 一次（无 per-CGU 回调）→ forge 侧消费 `collect_and_partition_mono_items` 的真分区：每 CGU 组内按符号名排序 + 独立 ObjectWriter 依次写 `forge_codegen_output.{cgu}.o`（对象按 CGU 名序、组内 .text 按符号序——确定）；函数任务沿用 M4 par_map（跨 CGU 上 rustc 池）；实例（Fn/Static）跨 CGU 同名去重（LocalCopy/内联副本同实例可附着多 CGU——COFF 全局强符号只能定义一次：首 CGU 定义、其余走 UNDEF）；**数据 owner 归并**：vtable/promoted 记录按符号名（=内容稳定键）全局去重、owner=首见 CGU，仅 owner 对象落 .data/.rodata（否则跨对象重复强符号 LNK2005）；alloc runtime 随首 CGU 对象；join_codegen 每 CGU 一个 WorkProduct（`WorkProductId::from_cgu_name`）——CGU 级增量由此获得。门控：`-C debuginfo>=1`（B-v1：单 CU DWARF 不拆——add_dwarf 要求 reloc 目标同文件定义）或 `FORGE_SINGLE_OBJECT=1` 回退单对象（cgu=1 单对象锚）。验证：e2e 3/3（103/103 + 并行池 + cargo 模板 cgu=4 exit=45）、新增 e2e_multi_object_cgu_units（多模块 cgu=4 → ≥2 .o、串/并行 exit 一致）、determinism 矩阵改口径（同配置逐对象字节全等 + 跨配置 exit 一致——.o 布局由 CGU 分区定）、M5 符号测试聚合多对象（181 符号跨串并行全等）；MSVC link.exe 多 .obj 链接无 LNK2005 | M6/B-v2 已实施：debuginfo per-CGU CU（每有函数的 CGU 对象带独立 DWARF CU——多对象与 debuginfo 并存，取消单对象回退；cgu=1/单对象路径与 Stage A 逐字节等价，probe SHA 实证）；-C incremental 多对象三次编译（含源码变更）e2e 通过 |

| 编号 | 位置 | 约定 |
| --- | --- | --- |
| WA-12 | README 用法 | `no_main` + `#[no_mangle] extern "C" fn main() -> i32` 走 MSVC 默认入口，无需 `/ENTRY` |
| WA-13 | tests/e2e.rs | 诊断输出全部 `FORGE_TRACE_*` env 门控（trace.rs 登记），不污染正常 stderr |
| WA-14 | lower/intrinsics.rs `write_bytes` + 主库 lowering `mem_opsize_from_type` + forge-rustc `rvalue.rs` cast mask | write_bytes 内联循环越界读写（e2e `write_bytes_loop`）+ **2026-08 已修并转正**：①块参数传参两层（map_terminator 复用 arg 映射 + pre_allocate 顺序）；②**窄类型宽度**——主库 Load/Store 用真实内存宽度（u8 读/写 1 字节不再越界 4 字节，`mem_opsize_from_type` 不枚举不截断、自定义非常规宽度原样传递）+ forge-rustc IntToInt cast 对 u8/u16 无符号源零扩展 mask——`write_bytes_loop` PASS exit=342（e2e 56/58）+ 已关闭 |
